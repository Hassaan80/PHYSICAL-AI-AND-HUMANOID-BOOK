# API Contracts

This feature is implemented as a command-line script for data ingestion. It does not expose any APIs. Therefore, there are no API contracts (e.g., OpenAPI, GraphQL) for this feature.

The "contract" for this feature is the command-line interface and the environment variables it requires, which will be documented in the `quickstart.md` file.
